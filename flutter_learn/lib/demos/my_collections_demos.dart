import 'package:flutter/material.dart';

class MyCollectionsDemos extends StatelessWidget {
  const MyCollectionsDemos({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      body: const Center(
        child: Text('dsfsdf'),
      ),
    );
  }
}
